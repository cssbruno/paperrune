version: "2"

run:
  timeout: 5m
  tests: true

linters:
  default: none
  enable:
    - bodyclose
    - copyloopvar
    - errcheck
    - errchkjson
    - errorlint
    - govet
    - ineffassign
    - nilerr
    - nilnesserr
    - staticcheck
    - wastedassign
